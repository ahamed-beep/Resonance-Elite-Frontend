// app/admin/login/layout.jsx

export const metadata = {
  title: "Admin Login | Resonance Elite",
  robots: { index: false, follow: false },
};

export default function AdminLoginLayout({ children }) {
  return children;
}